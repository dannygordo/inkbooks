// Single entry point apps/web (and, later, apps/mobile) import from - keeps the generated file's
// path an internal implementation detail of this package rather than something every consumer
// needs to know.
export * from "./generated/graphql";
